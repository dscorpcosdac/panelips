{% extends '::base.html.twig' %}
{% block title %}.::Etiquetas::.!{% endblock %}
{% block contenido -%}
<style type="text/css">
    a.dim {
display: inline-block;
color: #fff;
text-decoration: none;
text-transform: uppercase;
text-align: center;
padding-top: 6px;
margin-right: 10px;
position: relative;
cursor: pointer;
border-radius: 5px;
font-weight: 600;
margin-bottom: 20px !important;
box-shadow: inset 0px 0px 0px #16987e, 0px 5px 0px 0px #16987e, 0px 10px 5px #999999;
}

</style>
     <div class="row wrapper border-bottom white-bg page-heading">
                <div class="col-lg-10">
                    <h2>Macs</h2>
                    <ol class="breadcrumb">
                        <li>
                            <a href="index.html">Home</a>
                        </li>
                        <li>
                            <a>Listado</a>
                        </li>
                        <li class="active">
                            <strong>Macs</strong>
                        </li>
                    </ol>
                </div>
                <div class="col-lg-2">

                </div>
            </div>

 <div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
                <div class="col-lg-6">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>Importar excel</h5>
                          <a href="{{ path('homepage') }}" style="margin-left: 70px;" class="btn btn-danger btn-circle" title="Regresar al listado">
                                 <i class="fa fa-arrow-circle-o-left"></i>
                             </a>    
                        <div class="ibox-tools">                         
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                                                     
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">
                    {{ form_start(form, {'attr': {'class': 'form_transaccion_edit','action':  path('maclist_import')}}) }}
                  

                        <div class="form-group">
                            <label class="col-sm-2 control-label">Archivo .xls</label>
                                <div class="col-sm-10">
                                    <label title="Upload image file" for="inputImage" class="btn btn-primary">
                                    {{ form_widget(form.file) }}
                                    Upload new image
                                    </label>
                                    <span class="help-block m-b-none">{{ form_errors(form.file) }}</span>
                                </div>
                        
                        <div class="hr-line-dashed"></div>
                        <a href="{{ path('homepage') }}" class="btn btn-w-m btn-warning"><i class="fa fa-warning"></i> Cancelar</a>
                        <button type="submit" class="btn btn-primary" data-original-title="Guardar transaccion"><i class="fa fa-plus-square"></i> Guardar</button>
                    {{ form_end(form) }}
                   
            </div>
            </div>
            </div>
            </div>
             <div class="col-lg-6">
                                <div class="ibox float-e-margins">
                                    <div class="ibox-title">
                                        <h5>Servicios</h5>
                                        <div class="ibox-tools">
                                            <a class="collapse-link">
                                                <i class="fa fa-chevron-up"></i>
                                            </a>
                                            <a class="close-link">
                                                <i class="fa fa-times"></i>
                                            </a>
                                        </div>
                                    </div>
                                    <div class="ibox-content">
                                          <button class="btn btn-primary dim" type="button" onclick="consola(4)"><i class="fa fa-check" ></i> Aplicar cambios </button>
                                         
                                          <a href="{{path('maclist_export')}}" id="hrExport" class="btn btn-primary dim" target="_blank"><i class="fa fa-check" ></i> Exportar xls </a>
                                        
                                    </div>
                                </div>
                            </div>
               <div class="row">
                <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>Listado de Excepciones</h5>
                          <a href="{{path('exeption_new')}}"  style="margin-left: 70px;" class="btn btn-info btn-circle" title="Agregar equipo">
                                  <i class="fa fa-check"></i>
                             </a>    
                        <div class="ibox-tools">                         
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                                                     
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

       <table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
            <tr>
                <th>Id</th>  
                <th>IP ADDRESSES</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody id="bodyPolizaesepcion">
        {% set foo = 1 %}
        {% for entity in esepciones %}
            <tr id="ctnTr{{entity.id}}">
                <td>{{foo}}</td>           
               
                <td>
                    {% if entity.ip is defined %}
                        <input type="text" name="txtIp" id="txtIp{{entity.id}}" value="{{ entity.ip }}"><span style="display:none">{{ entity.ip }}</span>
                    {% else %}
                        <input type="text" name="txtIp" id="txtIp{{entity.id}}" value=""><span style="display:none"></span>
                    {% endif %}
                </td>
                <td>
                    <input type="hidden" name="txtElid" id="txtElid{{entity.id}}"  value="{{entity.id}}">
                     <button type="submit" class="btn btn-info form_esepcion_edit" entidad="{{entity.id}}" data-original-title="Modificar Entrada"><i class="fa fa-refresh"></i>
                     </button>
                    <button type="submit" class="btn btn-danger form_esepcion_delete" entidad="{{entity.id}}" data-original-title="Eliminar Entrada"><i class="fa fa-trash-o"></i>
                     </button>
               
                </td>
            </tr>
             {% set foo = foo + 1 %}          
        {% endfor %}
        </tbody>
     <tfoot>
             <tr>
                <th>Id</th>
                <th>IP ADDRESSES</th>
                <th>Actions</th>
            </tr>
            </tfoot>
            </table>

            </div>
            </div>
            </div>
            </div>
            <div class="row">
                <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>Listado de Macs</h5>
                          <a href="{{path('maclist_new')}}"  style="margin-left: 70px;" class="btn btn-info btn-circle" title="Agregar equipo">
                                  <i class="fa fa-check"></i>
                             </a>    
                        <div class="ibox-tools">                         
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                                                     
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

       <table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
            <tr>
                <th>Id</th>                
                <th>MAC</th>
                <th>IP ADDRESSES</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody id="bodyPoliza">
        {% set foo = 1 %}
        {% for entity in entities %}
            <tr id="ctnTr{{entity.id}}">
                <td>{{foo}}</td>              
                <td>
                    {% if entity.mac is defined %}
                        <input type="text" name="txtMac" id="txtMac{{entity.id}}" value="{{ entity.mac }}"><span style="display:none">{{ entity.mac }}</span>
                    {% else %}
                        <input type="text" name="txtMac" id="txtMac{{entity.id}}" value=""><span style="display:none"></span>
                    {% endif %}
                </td>
                <td>
                    {% if entity.ip is defined %}
                        <input type="text" name="txtIp" id="txtIp{{entity.id}}" value="{{ entity.ip }}"><span style="display:none">{{ entity.ip }}</span>
                    {% else %}
                        <input type="text" name="txtIp" id="txtIp{{entity.id}}" value=""><span style="display:none"></span>
                    {% endif %}
                </td>
                <td>
                    <input type="hidden" name="txtElid" id="txtElid{{entity.id}}"  value="{{entity.id}}">
                     <button type="submit" class="btn btn-info form_transaccion_edit" entidad="{{entity.id}}" data-original-title="Modificar Entrada"><i class="fa fa-refresh"></i>
                     </button>
                    <button type="submit" class="btn btn-danger form_transaccion_delete" entidad="{{entity.id}}" data-original-title="Eliminar Entrada"><i class="fa fa-trash-o"></i>
                     </button>
               
                </td>
            </tr>
             {% set foo = foo + 1 %}          
        {% endfor %}
        </tbody>
     <tfoot>
             <tr>
                <th>Id</th>
                <th>MAC</th>
                <th>IP ADDRESSES</th>
                <th>Actions</th>
            </tr>
            </tfoot>
            </table>

            </div>
            </div>
            </div>
            </div>

        <form id="frmDelete" method="POST" action="{{path('maclist_delete')}}"><input type="hidden" name="cualid" id="cualid"></form>

 {% endblock %}
